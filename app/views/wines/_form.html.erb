<%= simple_form_for(@wine) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.collection_radio_buttons(:winetype_ids, @winetypes, :id, :name) %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>


<% end %>
